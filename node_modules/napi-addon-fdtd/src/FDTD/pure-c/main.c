#include "./FDTD-2D.h"

// gcc main.c FDTD-2D.c -o main -lm
// https://russianblogs.com/article/59971148038/ - about exp and -lm

// https://russianblogs.com/article/26981495112/

// CURRENTLY NOT WORKINg!!!!!!!!!!!!!!!!!! problem with lingking

int main(int argc, char *argv[]) {
//   DATA_STRUCT *data_struct = (DATA_STRUCT *)malloc(sizeof(DATA_STRUCT));

//   set_params(data_struct);

//   // calc time layers
//   double *vect_x = (double *)malloc(data_struct->Nx * sizeof(double));
//   double *vect_y = (double *)malloc(data_struct->Nx * sizeof(double));

//   for (int i = 0; i < 100; ++i) {
//     calculate_next_time_layer(data_struct, vect_x, vect_y);
//   }

// //   show_data(vect_y, data_struct->Nx);

//   // unsigned decimal
//   printf("%zu", data_struct->ticks);

//   /* Free memory */
//   free(vect_x);
//   free(vect_y);

//   free(data_struct->eps);
//   free(data_struct->H1);
//   free(data_struct->H2);
//   free(data_struct->E1);
//   free(data_struct->E2);
//   free(data_struct);

  return 0;
}